# FastAPI and server
fastapi>=0.115.12
uvicorn[standard]==0.45.0
python-multipart>=0.0.22
pydantic>=2.13.3
pydantic-settings==2.14.0

# Session signing (PyJWT used for session cookies)
PyJWT[crypto]>=2.12.1

# HTTP
requests>=2.33.1
httpx>=0.27.0

# Email (Resend SDK — used by license email)
resend>=2.0.0

# Timezone data (python:3.13-slim strips backward-compatible tz symlinks)
tzdata>=2026.2

# Environment variables
python-dotenv==1.2.2

# Data processing (numpy used by embedding_utils)
numpy>=1.24.0

# AWS SDK (for DynamoDB)
boto3>=1.35.0
# Rate limiting
slowapi>=0.1.9

# Language detection (for news deduplication scoring)
langdetect==1.0.9

# Supabase / PostgreSQL (asyncpg connection pool for Supabase adapters)
asyncpg>=0.31.0
# Supabase Python client (async admin API for auth.users email lookup)
supabase>=2.0.0
